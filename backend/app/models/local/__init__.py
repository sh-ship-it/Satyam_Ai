"""On-prem model implementations (local lane).

These are interface-complete STUBS. Drop in real weights later — vLLM/Ollama for
the LLM, sentence-transformers BGE-M3 for embeddings/rerank, faster-whisper /
IndicConformer for STT, Indic-Parler-TTS for TTS — without changing the pipeline.
"""
